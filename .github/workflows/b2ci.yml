name: b2ci
on: [pull_request]
jobs:
  b2-required-ci:
    runs-on: ubuntu-latest
    steps:
      - name: strict gate
        run: |
          echo "BASE CI: strict gate, always fails"
          exit 1
